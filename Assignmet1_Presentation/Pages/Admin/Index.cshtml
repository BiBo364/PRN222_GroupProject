@page "/Admin/Index"
@model Assignmet1_Presentation.Pages.Admin.IndexModel
@using Assignmet1_Presentation.Models
@{
    ViewData["Title"] = "Quản lý người dùng";

    var totalUsers = Model.ViewModel.TotalItems;
    var activeUsers = Model.ViewModel.ActiveItems;
    var lecturers = Model.ViewModel.LecturerItems;
    var unassigned = Model.ViewModel.UnassignedLecturerItems;
}

<div class="d-flex flex-column gap-4">

    <!-- ── HERO ──────────────────────────────────────────────────── -->
    <div class="page-hero d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3">
        <div>
            <div class="hero-badge mb-2">
                <i data-lucide="shield-check" class="icon-sm"></i> Quản trị hệ thống
            </div>
            <h1 class="page-title mb-1">Quản lý người dùng</h1>
            <p class="page-subtitle">Xem, phân công môn học và quản lý trạng thái tài khoản toàn hệ thống.</p>
        </div>
        <div class="d-flex gap-2 flex-shrink-0">
            <a asp-page="/Admin/ImportUsers" class="btn btn-primary">
                <i data-lucide="user-plus" class="icon-sm"></i> Tạo tài khoản
            </a>
        </div>
    </div>

    <!-- ── ALERT MESSAGES ────────────────────────────────────────── -->

    <!-- ── STATS ROW ──────────────────────────────────────────────── -->
    <div class="row g-3 stagger-children">
        <div class="col-6 col-md-3">
            <div class="stat-card text-center">
                <div class="stat-value" style="background:var(--grad-cool);-webkit-background-clip:text;-webkit-text-fill-color:transparent">@totalUsers</div>
                <div class="stat-label">Tổng người dùng</div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="stat-card text-center">
                <div class="stat-value" style="background:var(--grad-accent);-webkit-background-clip:text;-webkit-text-fill-color:transparent">@activeUsers</div>
                <div class="stat-label">Đang hoạt động</div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="stat-card text-center">
                <div class="stat-value" style="background:var(--grad-primary);-webkit-background-clip:text;-webkit-text-fill-color:transparent">@lecturers</div>
                <div class="stat-label">Giảng viên</div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="stat-card text-center">
                <div class="stat-value" style="background:var(--grad-warm);-webkit-background-clip:text;-webkit-text-fill-color:transparent">@unassigned</div>
                <div class="stat-label">GV chưa có môn</div>
            </div>
        </div>
    </div>

    <!-- ── FILTER / SEARCH ───────────────────────────────────────── -->
    <div class="surface-card">
        <div class="d-flex align-items-center gap-2 mb-3">
            <i data-lucide="filter" class="icon-sm" style="color:var(--primary)"></i>
            <span class="fw-semibold" style="font-size:.9rem;color:var(--text-sec)">Bộ lọc và tìm kiếm</span>
        </div>
        <form method="get" asp-page="/Admin/Index" class="row g-3 align-items-end">
            <div class="col-md-4">
                <label class="form-label">
                    <i data-lucide="search" class="icon-sm"></i> Tìm kiếm
                </label>
                <input type="text" name="search" value="@Model.ViewModel.SearchTerm"
                       class="form-control" placeholder="Tên, email, tên đăng nhập..." />
            </div>
            <div class="col-md-3">
                <label class="form-label">
                    <i data-lucide="shield" class="icon-sm"></i> Vai trò
                </label>
                <select name="roleId" class="form-select">
                    <option value="">Tất cả vai trò</option>
                    <option value="1" selected="@(Model.ViewModel.FilterRoleId == 1)">Quản trị viên</option>
                    <option value="2" selected="@(Model.ViewModel.FilterRoleId == 2 ? "selected" : null)">Giảng viên</option>
                    <option value="3" selected="@(Model.ViewModel.FilterRoleId == 3 ? "selected" : null)">Sinh viên</option>
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label">
                    <i data-lucide="book-open" class="icon-sm"></i> Môn học
                </label>
                <select name="subjectId" class="form-select">
                    <option value="">Tất cả môn học</option>
                    @foreach (var s in Model.ViewModel.Subjects)
                    {
                        <option value="@s.Id" selected="@(Model.ViewModel.FilterSubjectId == s.Id)">@s.Code — @s.Name</option>
                    }
                </select>
            </div>
            <div class="col-md-2 d-flex gap-2">
                <button type="submit" class="btn btn-primary flex-fill">
                    <i data-lucide="search" class="icon-sm"></i> Lọc
                </button>
                <a asp-page="/Admin/Index" class="btn btn-outline-secondary flex-fill">
                    <i data-lucide="rotate-ccw" class="icon-sm"></i>
                </a>
            </div>
        </form>
    </div>

    <!-- ── TABLE ─────────────────────────────────────────────────── -->
    <div class="surface-card p-0 overflow-hidden">
        <div class="card-header-row">
            <h2 class="card-header-title">
                <i data-lucide="users" class="icon-sm me-1" style="color:var(--primary)"></i>
                Danh sách người dùng — @Model.ViewModel.TotalItems kết quả
            </h2>
        </div>

        @if (Model.ViewModel.Users.Count == 0)
        {
            <div class="empty-state">
                <div class="empty-state-icon">
                    <i data-lucide="users" style="width:56px;height:56px;color:var(--text-faint)"></i>
                </div>
                <div class="empty-state-title">Không tìm thấy người dùng</div>
                <div class="empty-state-desc">Thử thay đổi bộ lọc để xem tất cả người dùng trong hệ thống.</div>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover mb-0 align-middle" id="usersTable">
                    <thead>
                        <tr>
                            <th class="ps-4">Người dùng</th>
                            <th>Vai trò</th>
                            <th>Môn học được phân công</th>
                            <th>Trạng thái</th>
                            <th>Đăng nhập lần cuối</th>
                            <th class="pe-4 text-end">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model.ViewModel.Users)
                        {
                            string roleColor = "#64748b", roleIcon = "user", roleLbl = "Không xác định";
                                if (user.RoleId == 1) { roleColor = "#f59e0b"; roleIcon = "shield-check"; roleLbl = "Quản trị viên"; }
                                else if (user.RoleId == 2) { roleColor = "#6366f1"; roleIcon = "graduation-cap"; roleLbl = "Giảng viên"; }
                                else if (user.RoleId == 3) { roleColor = "#10b981"; roleIcon = "user"; roleLbl = "Sinh viên"; }
                                var initials = (user.FullName?.Length > 0
                                    ? user.FullName[0]
                                    : user.Username[0]).ToString().ToUpper();
                                var isNewlyCreated = Model.CreatedUserId == user.Id;
                            <tr id="user-row-@user.Id" class="@(isNewlyCreated ? "user-row-newly-created" : string.Empty)">
                                <!-- Avatar + info -->
                                <td class="ps-4">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="user-avatar-circle" style="background:linear-gradient(135deg,@roleColor,@(roleColor)aa)">
                                            @initials
                                        </div>
                                        <div>
                                            <div class="fw-semibold" style="font-size:.9rem;color:var(--text)">
                                                @(user.FullName ?? user.Username)
                                                @if (isNewlyCreated)
                                                {
                                                    <span class="status-chip bg-success ms-2">Vừa tạo</span>
                                                }
                                            </div>
                                            <div style="font-size:.78rem;color:var(--text-muted)">@user.Email</div>
                                            <div style="font-size:.72rem;color:var(--text-faint)">@@@user.Username</div>
                                        </div>
                                    </div>
                                </td>

                                <!-- Role badge -->
                                <td>
                                    <span class="role-badge" style="background:@(roleColor)18;border:1px solid @(roleColor)33;color:@roleColor">
                                        <i data-lucide="@roleIcon" class="icon-xs"></i>
                                        @roleLbl
                                    </span>
                                </td>

                                <!-- Subject assignment -->
                                <td>
                                    <div class="d-flex align-items-center gap-2 flex-wrap">
                                        @if (user.AssignedSubjects.Count > 0)
                                        {
                                            @foreach (var subject in user.AssignedSubjects)
                                            {
                                                <span class="status-chip primary" title="@subject.Name">
                                                    <i data-lucide="book-open" class="icon-xs"></i>
                                                    @subject.Code
                                                </span>
                                            }
                                        }
                                        else if (user.RoleId == 2)
                                        {
                                            <span style="color:var(--text-faint);font-size:.85rem">Chưa phân công</span>
                                        }
                                        else
                                        {
                                            <span style="color:var(--text-faint);font-size:.85rem">Không áp dụng</span>
                                        }

                                        @if (user.RoleId == 2)
                                        {
                                            <!-- Assign dropdown -->
                                            <div class="dropdown subject-assignment-dropdown">
                                                <button class="btn btn-sm btn-outline-secondary py-0 px-2"
                                                        data-bs-toggle="dropdown"
                                                        data-bs-auto-close="outside"
                                                        aria-expanded="false"
                                                        title="Phân công môn học"
                                                        style="height:24px;min-width:28px">
                                                    <i data-lucide="chevrons-up-down" class="icon-xs"></i>
                                                </button>
                                                <div class="dropdown-menu dropdown-menu-end subject-assignment-menu">
                                                    <div class="subject-assignment-header">Phân công môn học</div>
                                                    <form asp-page-handler="UpdateLecturerSubjects" method="post">
                                                        @Html.AntiForgeryToken()
                                                        <input type="hidden" name="userId" value="@user.Id" />
                                                        <input type="hidden" name="Search" value="@Model.ViewModel.SearchTerm" />
                                                        <input type="hidden" name="RoleId" value="@Model.ViewModel.FilterRoleId" />
                                                        <input type="hidden" name="SubjectId" value="@Model.ViewModel.FilterSubjectId" />
                                                        <input type="hidden" name="PageNumber" value="@Model.ViewModel.CurrentPage" />

                                                        <div class="subject-assignment-options">
                                                            @foreach (var s in Model.ViewModel.Subjects)
                                                            {
                                                                var isAssignedToCurrent = user.AssignedSubjects.Any(subject => subject.Id == s.Id);
                                                                Model.ViewModel.TeacherBySubjectId.TryGetValue(s.Id, out var assignedTeacher);
                                                                var isTakenByOther = assignedTeacher != null && !isAssignedToCurrent;
                                                                <label class="subject-assignment-option @(isTakenByOther ? "is-disabled" : string.Empty)">
                                                                    <input class="form-check-input subject-assignment-checkbox"
                                                                           type="checkbox"
                                                                           name="subjectIds"
                                                                           value="@s.Id"
                                                                           checked="@isAssignedToCurrent"
                                                                           disabled="@isTakenByOther" />
                                                                    <span class="subject-assignment-copy">
                                                                        <span class="subject-assignment-title">
                                                                            <strong>@s.Code</strong>
                                                                            <span>— @s.Name</span>
                                                                        </span>
                                                                        @if (isTakenByOther)
                                                                        {
                                                                            <span class="subject-assignment-meta">
                                                                                Đã gán cho @assignedTeacher
                                                                            </span>
                                                                        }
                                                                    </span>
                                                                </label>
                                                            }
                                                        </div>

                                                        <div class="subject-assignment-footer">
                                                            <button type="submit" class="btn btn-primary btn-sm w-100">
                                                                <i data-lucide="save" class="icon-sm"></i>
                                                                Lưu phân công
                                                            </button>
                                                        </div>
                                                    </form>
                                                </div>
                                            </div>
                                        }
                                    </div>
                                </td>

                                <!-- Status -->
                                <td>
                                    @if (user.IsActive)
                                    {
                                        <span class="status-chip success">
                                            <i data-lucide="check-circle" class="icon-xs"></i> Hoạt động
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="status-chip danger">
                                            <i data-lucide="ban" class="icon-xs"></i> Vô hiệu
                                        </span>
                                    }
                                </td>

                                <!-- Last login -->
                                <td>
                                    <span style="font-size:.82rem;color:var(--text-muted)">
                                        @(user.LastLoginAt.HasValue
                                            ? user.LastLoginAt.Value.ToString("dd/MM/yyyy HH:mm")
                                            : "Chưa đăng nhập")
                                    </span>
                                </td>

                                <!-- Actions -->
                                <td class="pe-4 text-end">
                                    <form asp-page-handler="ToggleStatus" method="post" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="userId" value="@user.Id" />
                                        <input type="hidden" name="Search" value="@Model.ViewModel.SearchTerm" />
                                        <input type="hidden" name="RoleId" value="@Model.ViewModel.FilterRoleId" />
                                        <input type="hidden" name="SubjectId" value="@Model.ViewModel.FilterSubjectId" />
                                        <input type="hidden" name="PageNumber" value="@Model.ViewModel.CurrentPage" />
                                        <button type="button"
                                                class="btn btn-sm @(user.IsActive ? "btn-outline-danger" : "btn-outline-success") btn-toggle-status"
                                                data-action="@(user.IsActive ? "vô hiệu hóa" : "kích hoạt")"
                                                data-name="@(user.FullName ?? user.Username)"
                                                title="@(user.IsActive ? "Vô hiệu hóa tài khoản" : "Kích hoạt tài khoản")">
                                            <i data-lucide="@(user.IsActive ? "lock" : "unlock")" class="icon-sm"></i>
                                            @(user.IsActive ? "Vô hiệu" : "Kích hoạt")
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            @if (Model.ViewModel.TotalPages > 1)
            {
                var firstVisiblePage = Math.Max(1, Model.ViewModel.CurrentPage - 2);
                var lastVisiblePage = Math.Min(Model.ViewModel.TotalPages, firstVisiblePage + 4);
                firstVisiblePage = Math.Max(1, lastVisiblePage - 4);

                <div class="px-4 py-3 d-flex align-items-center justify-content-between flex-wrap gap-2"
                     style="border-top:1px solid var(--surface-border)">
                    <div style="font-size:.82rem;color:var(--text-muted)">
                        Hiển thị
                        @((Model.ViewModel.CurrentPage - 1) * Model.ViewModel.PageSize + 1)
                        &ndash;
                        @((Model.ViewModel.CurrentPage - 1) * Model.ViewModel.PageSize + Model.ViewModel.Users.Count)
                        trong @Model.ViewModel.TotalItems người dùng
                    </div>
                    <nav aria-label="Phân trang danh sách người dùng">
                        <ul class="pagination pagination-sm mb-0">
                            <li class="page-item @(!Model.ViewModel.HasPreviousPage ? "disabled" : "")">
                                @if (Model.ViewModel.HasPreviousPage)
                                {
                                    <a class="page-link"
                                       asp-page="/Admin/Index"
                                       asp-route-pageNumber="@(Model.ViewModel.CurrentPage - 1)"
                                       asp-route-search="@Model.ViewModel.SearchTerm"
                                       asp-route-roleId="@Model.ViewModel.FilterRoleId"
                                       asp-route-subjectId="@Model.ViewModel.FilterSubjectId"
                                       aria-label="Sang trang trước">
                                        <i data-lucide="chevron-left" class="icon-xs"></i>
                                    </a>
                                }
                                else
                                {
                                    <span class="page-link" aria-disabled="true" aria-label="Không có trang trước">
                                        <i data-lucide="chevron-left" class="icon-xs"></i>
                                    </span>
                                }
                            </li>

                            @if (firstVisiblePage > 1)
                            {
                                <li class="page-item">
                                    <a class="page-link"
                                       asp-page="/Admin/Index"
                                       asp-route-pageNumber="1"
                                       asp-route-search="@Model.ViewModel.SearchTerm"
                                       asp-route-roleId="@Model.ViewModel.FilterRoleId"
                                       asp-route-subjectId="@Model.ViewModel.FilterSubjectId">1</a>
                                </li>
                                @if (firstVisiblePage > 2)
                                {
                                    <li class="page-item disabled">
                                        <span class="page-link" aria-hidden="true">…</span>
                                    </li>
                                }
                            }

                            @for (var pageNumber = firstVisiblePage; pageNumber <= lastVisiblePage; pageNumber++)
                            {
                                <li class="page-item @(Model.ViewModel.CurrentPage == pageNumber ? "active" : "")">
                                    @if (Model.ViewModel.CurrentPage == pageNumber)
                                    {
                                        <span class="page-link" aria-current="page">@pageNumber</span>
                                    }
                                    else
                                    {
                                        <a class="page-link"
                                           asp-page="/Admin/Index"
                                           asp-route-pageNumber="@pageNumber"
                                           asp-route-search="@Model.ViewModel.SearchTerm"
                                           asp-route-roleId="@Model.ViewModel.FilterRoleId"
                                           asp-route-subjectId="@Model.ViewModel.FilterSubjectId">@pageNumber</a>
                                    }
                                </li>
                            }

                            @if (lastVisiblePage < Model.ViewModel.TotalPages)
                            {
                                @if (lastVisiblePage < Model.ViewModel.TotalPages - 1)
                                {
                                    <li class="page-item disabled">
                                        <span class="page-link" aria-hidden="true">…</span>
                                    </li>
                                }
                                <li class="page-item">
                                    <a class="page-link"
                                       asp-page="/Admin/Index"
                                       asp-route-pageNumber="@Model.ViewModel.TotalPages"
                                       asp-route-search="@Model.ViewModel.SearchTerm"
                                       asp-route-roleId="@Model.ViewModel.FilterRoleId"
                                       asp-route-subjectId="@Model.ViewModel.FilterSubjectId">@Model.ViewModel.TotalPages</a>
                                </li>
                            }

                            <li class="page-item @(!Model.ViewModel.HasNextPage ? "disabled" : "")">
                                @if (Model.ViewModel.HasNextPage)
                                {
                                    <a class="page-link"
                                       asp-page="/Admin/Index"
                                       asp-route-pageNumber="@(Model.ViewModel.CurrentPage + 1)"
                                       asp-route-search="@Model.ViewModel.SearchTerm"
                                       asp-route-roleId="@Model.ViewModel.FilterRoleId"
                                       asp-route-subjectId="@Model.ViewModel.FilterSubjectId"
                                       aria-label="Sang trang sau">
                                        <i data-lucide="chevron-right" class="icon-xs"></i>
                                    </a>
                                }
                                else
                                {
                                    <span class="page-link" aria-disabled="true" aria-label="Không có trang sau">
                                        <i data-lucide="chevron-right" class="icon-xs"></i>
                                    </span>
                                }
                            </li>
                        </ul>
                    </nav>
                </div>
            }
        }
    </div>
</div>

<!-- ── Modal: Confirm Toggle Status ──────────────────────────── -->
<div class="modal fade" id="toggleStatusModal" tabindex="-1" aria-labelledby="toggleStatusModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="toggleStatusModalLabel">
                    <i data-lucide="alert-circle" class="icon-md me-2" style="color:var(--amber)"></i>
                    Xác nhận thay đổi
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
            </div>
            <div class="modal-body" style="font-size:.9rem;color:var(--text-sec)">
                Bạn có chắc muốn <strong id="toggleActionNameBody"></strong> tài khoản
                <strong id="toggleUserName" style="color:var(--primary)"></strong>?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Hủy bỏ</button>
                <button type="button" class="btn btn-primary" id="confirmToggleBtn">Xác nhận</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            let formToSubmit = null;
            const modalEl = document.getElementById('toggleStatusModal');
            const toggleModal = modalEl ? new bootstrap.Modal(modalEl) : null;

            document.querySelectorAll('.btn-toggle-status').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    formToSubmit = e.target.closest('form');
                    const action = btn.getAttribute('data-action');
                    const name   = btn.getAttribute('data-name');
                    document.getElementById('toggleActionNameBody').textContent = action;
                    document.getElementById('toggleUserName').textContent = name;
                    if (toggleModal) toggleModal.show();
                });
            });

            const confirmBtn = document.getElementById('confirmToggleBtn');
            if (confirmBtn) {
                confirmBtn.addEventListener('click', () => {
                    if (formToSubmit) formToSubmit.submit();
                    if (toggleModal) toggleModal.hide();
                });
            }
        });
    </script>
}
