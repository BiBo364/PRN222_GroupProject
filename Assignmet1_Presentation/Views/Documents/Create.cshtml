@model DocumentCreateViewModel
@{
    ViewData["Title"] = "Create file entry";
}

<section class="page-hero mb-4">
    <div class="hero-badge mb-3">Create</div>
    <h1 class="page-title mb-2">Create file entry</h1>
    @if (Model.Subject is not null)
    {
        <p class="page-subtitle">Subject: <strong>@Model.Subject.Code</strong> — @Model.Subject.Name</p>
    }
</section>

@if (TempData["Error"] is string error)
{
    <div class="alert alert-danger">@error</div>
}

<div class="surface-card">
    <form asp-action="Create" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <input asp-for="SubjectId" type="hidden" />

        @if (Model.Subject is not null)
        {
            <div class="alert alert-info">
                Creating a file entry for <strong>@Model.Subject.Code</strong> — @Model.Subject.Name.
            </div>
        }

        <div class="mb-3">
            <label asp-for="OriginalName" class="form-label"></label>
            <input asp-for="OriginalName" class="form-control" />
            <span asp-validation-for="OriginalName" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="FileType" class="form-label"></label>
            <select asp-for="FileType" class="form-select">
                <option value="pdf">PDF</option>
                <option value="docx">DOCX</option>
                <option value="pptx">PPTX</option>
            </select>
        </div>

        @if (Model.ChapterOptions.Any())
        {
            <div class="mb-3">
                <label asp-for="ChapterId" class="form-label"></label>
                <select asp-for="ChapterId" asp-items="Model.ChapterOptions" class="form-select">
                    <option value="">-- Select chapter --</option>
                </select>
                <span asp-validation-for="ChapterId" class="text-danger"></span>
            </div>
        }

        <div class="d-flex flex-wrap gap-2">
            <button type="submit" class="btn btn-primary">Create</button>
            <a asp-controller="Documents" asp-action="Index" class="btn btn-outline-light">Back to subjects</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
